{
  "buildCommand": "pnpm --filter @workspace/barcode-gen run build",
  "outputDirectory": "artifacts/barcode-gen/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
