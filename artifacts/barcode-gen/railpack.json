{
  "scripts": {
    "install": "pnpm install --no-frozen-lockfile",
    "build": "NODE_ENV=production pnpm --filter @workspace/barcode-gen run build"
  },
  "deploy": {
    "staticOutputDir": "dist/public"
  }
}
