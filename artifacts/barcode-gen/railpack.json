{
  "scripts": {
    "build": "cd ../.. && pnpm install --frozen-lockfile && NODE_ENV=production pnpm --filter @workspace/barcode-gen run build"
  },
  "deploy": {
    "staticOutputDir": "dist/public"
  }
}
